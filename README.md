# Corts Expert Performance Landing Site

Production target: `https://cortsexpert.hositee.com`
Reference identity/content source: `https://corts-e.sa/`

- Node 20+ / native HTTP (zero runtime dependencies)
- Riyadh-only campaign site
- GTM: `GTM-M9ZK36MB`
- Primary lead signal: `lead_form_success` after FormSubmit acknowledgement
- Secondary CTA signals: `click_whatsapp`, `click_call`
- No PHP files are used or modified on the original WordPress site.

## Health
`GET /healthz`
